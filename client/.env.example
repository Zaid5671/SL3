# Render backend URL (no trailing slash)
# Local dev: leave empty to use Vite proxy at /api
# Production example: https://shelflife-api.onrender.com
VITE_API_URL=

# Socket.IO URL (usually same as VITE_API_URL)
# Local dev: leave empty to default to http://localhost:5000
VITE_SOCKET_URL=
